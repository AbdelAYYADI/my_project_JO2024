{% extends 'base.html.twig' %}

{% block title %}Hello PasswordController!{% endblock %}

{% block body %}
    <div class="container w-25 border rounded bg-light-subtle form-control-sm form-label-sm mt-1">
        <h4 class="mt-3 mb-3 text-center"> Modifier votre mot de passe</h4>
        {{ form_start(formPassword) }}
            {{ form_row(formPassword.plainPassword)}}
        {{ form_end(formPassword) }}
    </div>
{% endblock %}
